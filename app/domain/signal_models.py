"""Signal domain models.

Placeholder module: the full signal/candidate value objects are implemented in
phase 8-10 (strategy, risk engine, signal lifecycle).  Kept intentionally
empty until then so no later-phase features are built prematurely.
"""

from __future__ import annotations
