"""Strategy research foundation (phase 8).

Deterministic, versioned, explainable research pipeline over immutable
snapshots.  Output is exclusively an internal SetupCandidate or a structured
SetupRejection - research artefacts for later phases, never trade signals,
never Telegram messages, and never any trade execution parameters.
"""
